import styles from '../Footer.module.css';

const ImportantNote = () => {
  return (
    <p className={styles.ImportantNote}>
      IMPORTANTE: TODOS LOS MONTOS DE BONIFICACIÓN SE MUESTRAN EN EL MONTO TOTAL DE LOS 10 PRIMEROS DEPÓSITOS.
    </p>
  );
};

export default ImportantNote;
